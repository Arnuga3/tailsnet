{
  "name": "b",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build": "npm install",
    "heroku-prebuild": "cd client && npm install && npm run-script build",

    "watch-server": "nodemon --watch auth --exec npm run-script start-server",
    "watch-client": "nodemon --watch client --exec npm run-script start"
  },
  "author": "arnuga3",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "passport": "^0.4.0",
    "passport-facebook": "^3.0.0",
    "passport-google-oauth": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^1.19.4"
  }
}
